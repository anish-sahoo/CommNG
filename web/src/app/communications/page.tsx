import ChannelLandingPage from "./components/channel-landing-page";

// Default comms landing now shows "My Channels".
export default function CommunicationsOverviewPage() {
  return <ChannelLandingPage variant="my" />;
}
