/**
 * Role types re-exported from backend for easy imports
 * This allows frontend code to import types without reaching into server directories
 */
import type { RoleKey } from "@server/data/roles";

export type { RoleKey };
